# Test Application Properties
spring.profiles.active=test

# H2 Database Configuration for Tests
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# SQL Script Initialization (disabled in tests)
spring.sql.init.mode=never

# Thymeleaf Configuration
spring.thymeleaf.cache=false

# Logging Configuration
logging.level.root=WARN
logging.level.com.gmg.sec30=INFO

# Mock Environment Variables for Tests
SPOTIFY_CLIENT_ID=test_client_id
SPOTIFY_CLIENT_SECRET=test_client_secret
YOUTUBE_API_KEY=test_youtube_api_key
# Test Profile Configuration
spring.application.name=sec30

# H2 Database Configuration for Tests
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# H2 Console (disabled in tests)
spring.h2.console.enabled=false

# SQL Script Initialization (disabled in tests)
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=false

# Thymeleaf Configuration
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.cache=false

# Logging Configuration
logging.level.root=INFO
logging.level.com.gmg.sec30=DEBUG
logging.level.org.springframework.security=INFO

# Test Environment Variables (mock values)
SPOTIFY_CLIENT_ID=test_client_id
SPOTIFY_CLIENT_SECRET=test_client_secret
YOUTUBE_API_KEY=test_youtube_api_key

